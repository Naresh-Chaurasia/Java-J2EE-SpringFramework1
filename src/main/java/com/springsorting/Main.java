package com.springsorting;

import java.util.Arrays;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.SpringApplication;
import org.springframework.context.annotation.AnnotationConfigApplicationContext;



public class Main {


    public static void main(String[] args) {
        int[] numbers = {5, 2, 9, 1, 3};

        //ApplicationContext applicationContext = SpringApplication.run(Main.class, args);
        
        //sortService.sortNumbers(numbers);

    //     // Use BubbleSort
    //     SortService bubbleSortService = new SortService(new BubbleSort());
    //     System.out.println("Bubble Sort: " + Arrays.toString(bubbleSortService.sortNumbers(numbers.clone())));

    //     // Use QuickSort
    //     SortService quickSortService = new SortService(new QuickSort());
    //     System.out.println("Quick Sort: " + Arrays.toString(quickSortService.sortNumbers(numbers.clone())));
    // }



    }
}
